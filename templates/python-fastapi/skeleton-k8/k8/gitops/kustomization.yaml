apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Namespace is created directly by the RHDH template (not managed by ArgoCD)
# to avoid a chicken-and-egg problem on first sync.

resources:
  - deployment.yaml
  - service.yaml
  - route.yaml
  - pipeline.yaml
