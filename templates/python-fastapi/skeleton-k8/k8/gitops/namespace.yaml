# Reference only — not included in kustomization.yaml.
# The template creates this namespace via kubernetes:create-namespace.
apiVersion: v1
kind: Namespace
metadata:
  name: ${{ values.namespace }}
  labels:
    argocd.argoproj.io/managed-by: openshift-gitops
